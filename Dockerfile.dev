FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package.json ./
RUN npm install

# Source code is mounted via volume, not copied
EXPOSE 5173

CMD ["npm", "run", "dev"]
